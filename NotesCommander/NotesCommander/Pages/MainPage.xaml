<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:NotesCommander.PageModels"
             xmlns:models="clr-namespace:NotesCommander.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="NotesCommander.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage"
             Title="Голосовые заметки">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedTo"
                Command="{Binding NavigatedToCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedFrom"
                Command="{Binding NavigatedFromCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="Loaded"
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Отладочная информация -->
        <VerticalStackLayout Grid.Row="0" Padding="10" BackgroundColor="LightYellow">
            <Label Text="=== ОТЛАДКА ===" FontSize="14" TextColor="Red" FontAttributes="Bold" />
            <Label Text="{Binding GroupedVoiceNotes.Count, StringFormat='GroupedVoiceNotes.Count={0}'}" FontSize="12" TextColor="Black" />
        </VerticalStackLayout>
        
        <ScrollView Grid.Row="1">
            <VerticalStackLayout>
                <!-- Простой список для отладки -->
                <CollectionView ItemsSource="{Binding GroupedVoiceNotes}"
                                Margin="10">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:VoiceNoteGroup">
                            <VerticalStackLayout BackgroundColor="LightBlue" Padding="5" Margin="5">
                                <Label Text="{Binding DateGroupDisplay}" FontAttributes="Bold" TextColor="Blue" />
                                <Label Text="{Binding Count, StringFormat='Items in group: {0}'}" TextColor="Green" />
                                
                                <!-- Список заметок в группе -->
                                <CollectionView ItemsSource="{Binding .}" Margin="10,0,0,0">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate x:DataType="models:VoiceNote">
                                            <Border BackgroundColor="White" Padding="10" Margin="5">
                                                <VerticalStackLayout>
                                                    <Label Text="{Binding Title}" FontAttributes="Bold" />
                                                    <Label Text="{Binding Duration, StringFormat='Duration: {0}'}" FontSize="12" />
                                                    <Label Text="{Binding RecognitionStatus, StringFormat='Status: {0}'}" FontSize="12" />
                                                    <Label Text="{Binding AudioFilePath, StringFormat='Audio: {0}'}" FontSize="10" />
                                                    <Button Text="▶ Play" 
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type pageModels:MainPageModel}}, Path=PlayAudioCommand}"
                                                            CommandParameter="{Binding .}"
                                                            BackgroundColor="Green"
                                                            TextColor="White"
                                                            Margin="0,5,0,0" />
                                                </VerticalStackLayout>
                                            </Border>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </VerticalStackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>
        
        <Button Grid.Row="2"
                Text="Добавить голосовую заметку"
                Margin="20,0,20,24"
                HeightRequest="48"
                Command="{Binding AddVoiceNoteCommand}"
                SemanticProperties.Description="Добавить голосовую заметку" />
    </Grid>
</ContentPage>
