<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:NotesCommander.PageModels"
             xmlns:models="clr-namespace:NotesCommander.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:fonts="clr-namespace:Fonts"
             x:Class="NotesCommander.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage"
             Title="Notes">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedTo"
                Command="{Binding NavigatedToCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedFrom"
                Command="{Binding NavigatedFromCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="Loaded"
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />

            <Style x:Key="PlayStopIconButtonStyle" TargetType="Button">
                <Setter Property="WidthRequest" Value="44" />
                <Setter Property="HeightRequest" Value="44" />
                <Setter Property="CornerRadius" Value="22" />
                <Setter Property="BackgroundColor" Value="#2E7D32" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontFamily" Value="{x:Static fonts:FluentUI.FontFamily}" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Text" Value="{x:Static fonts:FluentUI.play_20_regular}" />
                <Setter Property="SemanticProperties.Description" Value="Play voice note" />
                <Style.Triggers>
                    <DataTrigger TargetType="Button" Binding="{Binding IsPlaying}" Value="True">
                        <Setter Property="Text" Value="{x:Static fonts:FluentUI.stop_20_regular}" />
                        <Setter Property="BackgroundColor" Value="#C62828" />
                        <Setter Property="SemanticProperties.Description" Value="Stop voice note" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">
        <VerticalStackLayout Grid.Row="0" Padding="12" BackgroundColor="LightYellow">
            <Label Text="=== ОТЛАДКА ===" FontSize="14" TextColor="Red" FontAttributes="Bold" />
            <Label Text="{Binding GroupedVoiceNotes.Count, StringFormat='GroupedVoiceNotes.Count={0}'}" FontSize="12" TextColor="Black" />
            <Label Text="{Binding DebugVmName, StringFormat='VM: {0}'}" FontSize="12" TextColor="Black" />
            <Label Text="{Binding PlayCommandStatus}" FontSize="12" TextColor="DarkBlue" />
            <Label Text="{Binding PlayButtonClickedCount, StringFormat='Button clicked events: {0}'}" FontSize="12" TextColor="OrangeRed" />
            <Label Text="{Binding PlayAudioInvokedCount, StringFormat='Play command invoked: {0}'}" FontSize="12" TextColor="DarkGreen" />
            <Label Text="{Binding LastPlayAudioStatus, StringFormat='Last play status: {0}'}" FontSize="12" TextColor="DarkRed" LineBreakMode="TailTruncation" />
        </VerticalStackLayout>

        <Grid Grid.Row="1">
            <CollectionView ItemsSource="{Binding GroupedVoiceNotes}"
                            IsGrouped="True"
                            SelectionMode="None"
                            Margin="12,0,12,12">
                <CollectionView.GroupHeaderTemplate>
                    <DataTemplate x:DataType="models:VoiceNoteGroup">
                        <Border BackgroundColor="LightBlue" Padding="8" Margin="0,0,0,6">
                            <VerticalStackLayout>
                                <Label Text="{Binding DateGroupDisplay}"
                                       FontAttributes="Bold"
                                       FontSize="16"
                                       TextColor="Blue" />
                                <Label Text="{Binding Count, StringFormat='Items in group: {0}'}"
                                       FontSize="12"
                                       TextColor="Green" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.GroupHeaderTemplate>

                
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:VoiceNote">
                        <Border BackgroundColor="White" Padding="12" Margin="0,0,0,8" Stroke="#EEEEEE" StrokeThickness="1" >
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Button Style="{StaticResource PlayStopIconButtonStyle}"
                                        Clicked="OnPlayButtonClicked"
                                        VerticalOptions="Start" />

                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="{Binding Title}" FontAttributes="Bold" />
                                    <Label Text="{Binding DurationDisplay, StringFormat='Duration: {0}'}" FontSize="12" />
                                    <Label Text="{Binding RecognitionStatusDisplay, StringFormat='Status: {0}'}" FontSize="12" />
                                    <Label Text="{Binding AudioFilePath, StringFormat='Audio: {0}'}" FontSize="10" />
                                </VerticalStackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="+"
                    WidthRequest="56"
                    HeightRequest="56"
                    CornerRadius="28"
                    BackgroundColor="#5B3CC4"
                    TextColor="White"
                    FontSize="24"
                    FontAttributes="Bold"
                    HorizontalOptions="End"
                    VerticalOptions="End"
                    Margin="0,0,12,12"
                    Command="{Binding AddVoiceNoteCommand}"
                    SemanticProperties.Description="Добавить голосовую заметку" />
        </Grid>
    </Grid>
</ContentPage>
